state_id: "WV-STATE-LATEST-002"
ecosystem_name: "Wiltonverse"
current_stage: "WV_REPRODUCIBILITY_VALIDATION_RUNTIME_v1"
status: "READY_TO_COMPILE_AND_VERIFY"
truth_state: "DECLARED"
last_known_maturity_label: "Merged EXEC baseline -> Reproducibility & Validation Runtime"
core_reading:
  what_is_real_now:
    - "Constitutional governance logic exists"
    - "Truth policy exists"
    - "Cryptographic provenance intent and prior evidence exist"
    - "Minimal runtime pack exists"
    - "Environment check and ChatGPT export parser exist"
  what_is_missing_now:
    - "Successful local execution outside chat"
    - "State snapshot and evidence bundle generated by runtime"
    - "Independent reproduction on a second machine"
    - "Repeatable benchmark history"
next_mandatory_transition:
  from: "prepared execution pack"
  to: "local verified runtime with evidence bundle and external reproduction path"
promotion_rule:
  current_truth_state: "DECLARED"
  may_promote_to_partial_verified_only_if:
    - "check_environment.ps1 passes"
    - "compile_pack.ps1 executes successfully"
    - "build_state_snapshot.ps1 emits a structured snapshot"
    - "run_heartbeat.ps1 emits logs"
    - "run_benchmark.ps1 emits structured results"
    - "verify_pack.ps1 passes integrity checks"
    - "generate_evidence_bundle.ps1 creates a bundle with hashes"
  may_promote_to_verified_only_if:
    - "portable pack is reproduced by an independent operator or machine"
    - "verification_report.json confirms the reproduced bundle"
    - "hashes match expected inventory or expected canonical updates"
blockers:
  - "No runtime claim without logs"
  - "No verification claim without verification_report.json"
  - "No reproduction claim without second-machine evidence"
  - "No promotion without evidence artifacts"
